{% extends 'base.html.twig' %}

{% block title %}Admin - index des services{% endblock %}

{% block body %}
    <h1>Liste des utilisateurs</h1>

    <a href="{{ path('admin.service.create') }}">Créer un nouveau service</a>
    <table>
        <thead>
            <tr>
                <th>Service</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for service in services %}
                <tr>
                    <td>{{ service.name }}</td>
                    <td>
                        <a href="{{ path('admin.service.edit', {'id': service.id}) }}">Modifier</a>
                        <form method="post" action="{{ path('admin.service.delete', {'id': service.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer çe service ?')" style="display: inline;">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service.id) }}">
                            <button type="submit">Supprimer</button>
                        </form>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="2">Aucun service trouvé.</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}