{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
    <div class="center">
        <div class="form-list">
            <img src="{{ asset('images/logo-entreprise.jpg') }}" alt="Logo de l'entreprise.">
            <div class="btn-list">
                <a href="{{ path('app.register') }}" class="btn-home-1">Inscription</a>
                <a href="{{ path('app.login') }}" class="btn-home-2">Connexion</a>
            </div>
        </div>
    </div>
{% endblock %}