{% extends 'base.html.twig' %}

{% block title %}Create{% endblock %}

{% block body %}
    <h1>Créer des annonces</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Créer</button>
    {{ form_end(form) }}
{% endblock %}